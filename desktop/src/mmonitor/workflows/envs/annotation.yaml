# Snakemake environment for annotation rules
name: mmonitor-smk-annotation
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - python>=3.10
  - pandas
  - bakta>=1.12.0
  - diamond>=2.1.10
